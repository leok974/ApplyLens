# Analytics Pipeline Dependencies
# For dbt, BigQuery, and Elasticsearch integration

# dbt with BigQuery adapter
dbt-core==1.8.7
dbt-bigquery==1.8.7

# Google Cloud BigQuery (pinned for compatibility with dbt-bigquery)
google-cloud-bigquery==3.25.0

# Elasticsearch for analytics export
elasticsearch==8.15.0

# Python standard analytics libraries (optional, for custom scripts)
pandas>=2.1.0
numpy>=1.24.0
